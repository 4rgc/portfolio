export type ExperienceDataItem = {
  period: string;
  iconName: string;
  companyName: string;
  title: string;
  description: Array<string>;
};

export type ExperienceData = Array<ExperienceDataItem>;
